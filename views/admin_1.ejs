<!DOCTYPE HTML>
<html>
  <head>
    <meta charset="utf-8">
		<title>ADMIN_1</title>
    <style>
      * {margin:auto; text-align:center;}
      .title { text-align:center; font-weight: bolder; font-size:3em; margin: 30px 10px; }
			.subtitle { font-weight: bolder; font-size:1.5em; margin: 10px 10px;}
      .emphasis { color: blue; font-weight: bolder; margin:1%;}
      .tr_title { color: blue; font-weight: bolder; text-align:justify;}
      hr.top {margin-bottom:5%;}
      input{ text-align:center; width:80%;}
      table{ width: 80%; }
      td {width:20%;}
      button {color: blue; margin: 2% 5%; width: 20%; height: 10%;}
    </style>
  </head>

  <body>
		<div class="title">Admin_1  <span class="emphasis">카메라설정</span></div><hr><hr class="top">
<!-- res_cw / res_ch / res_rw / res_rh / num_cam -->
    <form action="/ADMIN_1/toADMIN_wait" method="get">
      <table>
        <tr>
        <td><div class="tr_title">카메라 해상도</div> </td><td><input type="text" name = "res_cw" class="input" placeholder="width" required></td><td><input type="text" name = "res_ch" placeholder="height" required></td>
        </tr><tr>
        <td><div class="tr_title">리사이즈 해상도</div> </td><td><input type="text" name = "res_rw" class="input" placeholder="width" required></td><td><input type="text" name = "res_rh" placeholder="height" required></td>
        </tr><tr>
        <td><div class="tr_title">카메라 숫자 </div></td><td><select name="num_cam" style="width:82%; ">
             <option value=1 class="input">1 <option value=2>2 <option value=3>3 <option value=4>4 <option value=5>5
        </select></td>
        </tr>
      </table><br>
      <button type="submit"> 다음 - 관심영역 설정 </button>
    </form>
    <% if(isSet) { %>
    <form action="/ADMIN_1/toBASIC" method="get">
      <button type="submit"> 뒤로가기 - 메인페이지로 </button>
    </form><br><br>
    <% } %>
  </body>
</html>
